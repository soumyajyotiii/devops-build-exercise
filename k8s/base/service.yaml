apiVersion: v1
kind: Service
metadata:
  name: underwriting-agent
  namespace: underwriting-agent
  labels:
    app.kubernetes.io/name: underwriting-agent
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: underwriting-agent
