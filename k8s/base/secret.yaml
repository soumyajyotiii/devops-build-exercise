# placeholder — in practice these come from external-secrets-operator
# syncing from AWS Secrets Manager. never commit real values here.
apiVersion: v1
kind: Secret
metadata:
  name: underwriting-agent-secrets
  namespace: underwriting-agent
type: Opaque
stringData:
  ANTHROPIC_API_KEY: "REPLACE_VIA_EXTERNAL_SECRETS"
  DATABASE_URL: "REPLACE_VIA_EXTERNAL_SECRETS"
