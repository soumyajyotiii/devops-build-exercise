apiVersion: v1
kind: ServiceAccount
metadata:
  name: underwriting-agent
  namespace: underwriting-agent
  annotations:
    # IRSA — map this k8s SA to an AWS IAM role for pod-level AWS access
    eks.amazonaws.com/role-arn: "arn:aws:iam::ACCOUNT_ID:role/underwriting-agent-pod-role"
